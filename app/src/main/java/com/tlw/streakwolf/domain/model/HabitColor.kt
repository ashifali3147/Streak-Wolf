package com.tlw.streakwolf.domain.model

/**
 * The fixed accent set a habit can be tinted with.
 *
 * Names only, no hex: what each one renders as is the theme's decision,
 * and differs between light and dark.
 */
enum class HabitColor {
    FLAME,
    CLAY,
    INK,
    TEAL,
    PLUM,
    SLATE,
}
